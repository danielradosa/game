# Shared by both server and frontend Docker builds (context = repo root).
# Service-specific exclusions live in each Dockerfile via narrow COPY commands.
node_modules
**/node_modules
dist
**/dist
.git
.DS_Store
*.log
.env
.env.*
!.env.example

# Plans / docs — never needed at runtime
.planning
docs
